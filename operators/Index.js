function simpleOperators (numberA, numberB){
 let adding = number  + number;
 let subtract = number - number;
 let multiply = number * number;
 let divide = number / number;
 console. log (adding);
 console. log (subtract);
 console. log (multiply);
 console. log (divide);
}

let number1 = [4, 7, 20, 35];
simpleOperators (number[0], number[1]);
simpleOperators (number[2], number[3]);
let skipcount
